# Moon Environment Profile
# ─────────────────────────────────────────────────────────────────────────────
# Optional. Fill in to get targeted intel recommendations mapped to your actual
# controls, log sources, and asset inventory. Leave empty for generic output.
# ─────────────────────────────────────────────────────────────────────────────

# org: Acme Corp
# industry: technology          # e.g. finance, healthcare, technology, retail
# employee_count: ~500

# ── Security Controls ────────────────────────────────────────────────────────
# controls:
#   endpoint:
#     - name: CrowdStrike Falcon
#       coverage: all workstations and servers
#   network:
#     - name: Cloudflare WAF
#       coverage: all public-facing endpoints
#   identity:
#     - name: Okta
#       coverage: all employees (MFA enforced)
#   cloud:
#     - name: AWS GuardDuty
#       coverage: all accounts
#   siem:
#     - name: Splunk
#       coverage: 90-day retention
#   gaps:
#     - No dark web monitoring

# ── Log Sources ───────────────────────────────────────────────────────────────
# log_sources:
#   - AWS CloudTrail (all regions)
#   - AWS VPC Flow Logs
#   - GitHub audit logs

# ── Asset Inventory ───────────────────────────────────────────────────────────
# assets:
#   cloud:
#     provider: AWS
#     regions: [us-east-1, us-west-2]
#     primary_services: [EC2, EKS, RDS, S3, Lambda]
#   critical_systems:
#     - name: Customer data store
#       notes: RDS PostgreSQL
#   public_attack_surface:
#     - Customer-facing web app

# ── Compliance & Frameworks ───────────────────────────────────────────────────
# compliance:
#   - SOC 2 Type II

# ── Patch SLAs ────────────────────────────────────────────────────────────────
# patch_slas:
#   critical: 24 hours
#   high: 7 days
#   medium: 30 days
